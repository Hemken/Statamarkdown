% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/purl_stata.R
\name{purl_stata}
\alias{purl_stata}
\title{Extract Stata code from a dynamic document}
\usage{
purl_stata(input, output = NULL, text = NULL, documentation = 1L)
}
\arguments{
\item{input}{A character string with the name of the input document.}

\item{output}{A character string with the name of the do-file to
write.  Defaults to the name of the input document with its
extension changed to \code{.do}.}

\item{text}{A character string with the document text to use in
place of a file.}

\item{documentation}{How much documentation to carry into the
do-file, following \code{\link[knitr:purl]{knitr::purl()}}: \code{0} (or \code{FALSE}) extracts the
code only; \code{1} (or \code{TRUE}, the default) precedes the code of each
chunk with a Stata comment giving the chunk's header (its label
and options); \code{2} also includes the document's text as Stata
comments (the code of non-Stata chunks is not included).}
}
\value{
If a do-file is written, the path to the do-file, invisibly.
If \code{text} is given and \code{output} is \code{NULL}, a character vector of
the extracted lines.
}
\description{
The Stata analogue of \code{\link[knitr:purl]{knitr::purl()}}: extracts the code from the
Stata code chunks of an R Markdown or Quarto document and writes it
to a Stata do-file.
}
\details{
Chunks are recognised with knitr's own chunk patterns, so indented
chunks and fences of more than three backticks are handled.  A chunk
is extracted when its header engine is \code{stata}, or when it uses the
older \code{r} chunk form with an \code{engine = "stata"} option.  Chunks with
the \code{purl = FALSE} or \code{eval = FALSE} options (either in the chunk
header or in option comments) are skipped.  Option comments are
split from the code by \code{\link[knitr:partition_chunk]{knitr::partition_chunk()}}, so the forms
knitr itself recognises are honoured: \verb{*|} in a \code{stata} chunk and
\verb{#|} in a chunk using the older \code{engine='stata'} form.  They are
never copied into the do-file as code, but with \code{documentation >= 1}
they are recorded as plain Stata comments below the chunk header
line.
}
\examples{
indoc <- '
Some text.

```{r}
library(Statamarkdown)
```

```{stata first-Stata, collectcode=TRUE}
sysuse auto, clear
generate gpm = 1/mpg
```

```{stata second-Stata}
regress price gpm
```
'
purl_stata(text = indoc)
}
\seealso{
\code{\link[knitr:purl]{knitr::purl()}}, \link{Statamarkdown-package}
}
